version: '3.8'

services:
  # PostgreSQL Database
  db:
    image: postgres:15-alpine
    container_name: hars-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: hars_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres123}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - hars-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend Server (prebuilt image from ECR)
  server:
    image: 025158345480.dkr.ecr.ap-northeast-2.amazonaws.com/hars-server:latest
    container_name: hars-server
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - .env
    environment:
      # Database connection
      DATABASE_URL: ${DATABASE_URL}
      # AWS credentials from .env
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
      AWS_REGION: ${AWS_REGION:-ap-northeast-2}
      S3_BUCKET_NAME: ${S3_BUCKET_NAME:-hars-submissions-henryhjna}
    ports:
      - "5000:5000"
    networks:
      hars-network:
        aliases:
          - server
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Frontend Client (prebuilt image from ECR)
  client:
    image: 025158345480.dkr.ecr.ap-northeast-2.amazonaws.com/hars-client:latest
    container_name: hars-client
    restart: unless-stopped
    depends_on:
      - server
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/letsencrypt/live/hanyanghars.com/fullchain.pem:/etc/nginx/ssl/fullchain.pem:ro
      - /etc/letsencrypt/live/hanyanghars.com/privkey.pem:/etc/nginx/ssl/privkey.pem:ro
    networks:
      - hars-network

networks:
  hars-network:
    driver: bridge

volumes:
  postgres_data:
    driver: local
